class FoodItem {
  String image;
  String name;
  bool isVeg;
  String price;
  bool isSelected;
  int count = 0;
  FoodItem(this.image, this.name, this.isVeg, this.price, this.isSelected,
      this.count);
}